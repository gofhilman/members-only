<main>
  <form action="/login" method="post">
    <h2 class="page-title"><%= title %></h2>
    <% if (locals.path) { switch(path) { case "/post": %>
    <p class="error-msg">Please log in to create a post</p>
    <% break; case "/join": %>
    <p class="error-msg">Log in first to apply for membership</p>
    <% break; case "/admin": %>
    <p class="error-msg">Please log in to apply for admin access</p>
    <% } } %> <%- include("partials/errors.ejs") %> <% if (locals.message) { %>
    <p class="error-msg"><%= message %></p>
    <% } %>
    <div class="form-body">
      <label for="username">Username</label>
      <input type="text" name="username" id="username" required />
      <label for="password">Password</label>
      <input type="password" name="password" id="password" required />
    </div>
    <button>Log in</button>
  </form>
  <p>Don't have an account? Sign up <a href="/signup">here</a>.</p>
</main>
